enum eScreens {
  HOME = 'Home',
  HISTORY = 'History',
}

enum eResults {
  RESULTS = 'Results',
}

enum eColors {
  WHITE = '#FFFFFF',
  BLACK = '#000000',
}

enum eImages {
  MENU = require('../../assets/menu_icon_light.png'),
  MOON = require('../../assets/moon_icon.png'),
  SUN = require('../../assets/sun_icon.png'),
  REFRESH = require('../../assets/refresh_icon.png'),
}

export { eScreens, eResults, eColors, eImages };
